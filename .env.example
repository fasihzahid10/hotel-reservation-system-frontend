# Leave default (relative /api) so the Next.js dev server can proxy to the backend; set a full URL only if the API is on another host.
# NEXT_PUBLIC_API_URL="http://localhost:4000/api"
COOKIE_NAME="hrs_access_token"
